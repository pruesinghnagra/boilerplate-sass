import React from 'react'
import ReactDOM from 'react-dom'

import App from '../../the-one-boilerplate/client/components/App'

document.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(
    <App/>,
    document.getElementById('app')
  )
})
